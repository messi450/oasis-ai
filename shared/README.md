# Shared Resources

This directory is for code, types, or constants shared between `frontend` and `backend`.

## Examples
- TypeScript Interfaces / Zod Schemas
- Constants (e.g., API status codes)
- Shared utility functions
